#include <iostream>
using namespace std;

int main() {
    // Please write your code here.
    int am, bm, ae, be;
    cin >> am >> bm >> ae >> be;

    cout << ((am >= bm) && (ae >= be));

    return 0;
}