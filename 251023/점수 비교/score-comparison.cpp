#include <iostream>
using namespace std;

int main() {
    // Please write your code here.
    int am, bm, ae, be;
    cin >> am >> ae >> bm >> be;

    cout << ((am > bm) && (ae > be));

    return 0;
}